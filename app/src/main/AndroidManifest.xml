<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.atlanticbakery">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main"></activity>
        <activity android:name=".CancelTransactionPOS" />
        <activity
            android:name=".Nav"
            android:label="@string/title_activity_nav" />
        <activity
            android:name=".CancelTransaction"
            android:parentActivityName=".MainMenu" />
        <activity
            android:name=".Inventory"
            android:parentActivityName=".MainMenu" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".IPAddress" />
        <activity
            android:name=".CreateUser"
            android:parentActivityName=".MainMenu" />
        <activity
            android:name=".ShoppingCart"
            android:parentActivityName=".MainMenu" />
        <activity
            android:name=".ScanCode"
            android:parentActivityName=".QRCode" />
        <activity
            android:name=".Received"
            android:parentActivityName=".MainMenu" />
        <activity android:name=".MainMenu" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>